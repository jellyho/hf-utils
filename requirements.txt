# Web app
fastapi>=0.111
uvicorn>=0.30

# LeRobot pins huggingface_hub to <0.36; keep ours in the same range so the
# two never fight during resolution.
huggingface_hub>=0.34,<0.36

# LeRobot dataset reading (viewer/editor) — pyarrow/pandas only, no torch needed.
pyarrow>=15
pandas>=2.0

# Video probing for the viewer, and a bundled ffmpeg for episode rendering
# (imageio-ffmpeg ships a static binary, so no system ffmpeg is required).
av>=12
imageio-ffmpeg>=0.5

# LeRobot dataset download/upload + structural edits (pulls torch, transformers, etc.).
lerobot==0.4.4
